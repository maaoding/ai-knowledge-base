{
  "name": "ai-knowledge-base",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs --host 127.0.0.1",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs --host 127.0.0.1",
    "docs:export": "node scripts/export-pages.mjs",
    "deploy:check": "npm run docs:export && node scripts/deploy-check.mjs"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
