---
description: '拆解 AI 绘画系统中的文本编码器、扩散模型、VAE、采样器、LoRA 与 ControlNet，理解各组件分工。'
---

# AI 绘画由哪些部分组成

> **一句话结论：** 一次文生图由底模、文本编码器、VAE、采样器与调度器、提示词、生成参数和各类插件共同完成；每个部件只负责一段流程，动它只影响它负责的那一段。

## 前置知识

- 已阅读 [底模是什么](/concepts/base-model)，知道底模在整个系统中的核心地位。
- 不要求用过 ComfyUI 或 WebUI；这份部件清单在主流工具里都成立，只是叫法和摆放位置不同。
- 先接受一个边界：本页讲“每个部件负责什么、动它会影响什么”，去噪算法在 [基本原理](/concepts/how-it-works) 中展开。

## 学习目标

读完后，你应该能够：

- 按顺序说出一幅文生图从提示词到成品经过哪些部件。
- 区分底模、文本编码器、VAE 这三个都叫“模型”的部件各管什么。
- 说明采样器、调度器、步数、CFG、seed 各自拧的是什么旋钮。
- 指出 LoRA 和 ControlNet 在流程中插入的位置。
- 对照常见节点名看懂一个 ComfyUI 文生图工作流。
- 用“固定 seed、单变量对照”的思路排查出图问题。
- 按影响大小给各组件排序，确定学习与排查的优先级。

## 核心概念

### 一张图的完整流水线是什么样

```text
提示词（正向 / 负向）
   → 文本编码器：把文字变成模型能理解的向量
   → 初始噪声：一片随机噪声（seed 决定它长什么样）
   → 采样器 + 调度器：在底模上分多步去噪（LoRA 在此叠加，ControlNet 在此注入约束）
   → 潜空间图像（latent）
   → VAE 解码：latent → 人眼可见的图片
```

记住顺序：文字先被读懂，噪声再被逐步清理，最后才翻译成图片。下一节先把这条流水线对应到 ComfyUI 的常见节点，之后再按环节展开各部件的分工。

每个部件的职责一句话：

- **底模（Checkpoint）**：提供生成能力本身，决定风格、知识范围、分辨率习惯和生态兼容性。
- **文本编码器（Text Encoder）**：把提示词翻译成向量。常见有 CLIP（OpenAI 发布的图文对照文本编码器）、T5（Google 的另一套文本编码模型）等，不同代模型可能用不同编码器；更新的模型开始直接用大语言模型或视觉语言模型充当文本编码器。
- **VAE（Variational Autoencoder，变分自编码器）**：在潜空间与像素图之间双向转换：编码进、解码出。
- **采样器（Sampler）**：决定每一步去噪怎么走。
- **调度器（Scheduler）**：决定噪声强度随步数怎么衰减，常与采样器成对出现。
- **LoRA**：挂在底模权重上的小型附加模型，补充角色、画风、服装等概念。
- **ControlNet**：注入线稿、深度、骨架等条件，约束构图和姿势，详见[ControlNet 入门](/concepts/controlnet)。
- **工作流（Workflow）**：把以上部件按顺序连接的执行流程，在 ComfyUI 里表现为节点图。
  - 常见形态：文生图、图生图、局部重绘、放大修复、参考图控制。
  - 同一个模型可以配多种工作流；工作流只是流程，模型能力仍是基础。

放大模型（Upscaler）是流水线末端的另一类角色：它不重新理解提示词，只在低分辨率图上补充细节、扩大尺寸。常见用法是先生成小图再放大，或在放大后低强度重绘补细节；相比直接生成大图，这条路对显存更友好。想上手时，可从[精选教程](/resources/tutorials)的 ComfyUI 官方示例工作流进入，那里有现成的放大示例。

### 对应到 ComfyUI 的常见节点

打开一个文生图工作流，上面这些部件大多各自对应一个节点，名称与职责对照如下（节点名以所用工具版本为准，部分界面会译成中文）：

| 流水线环节 | ComfyUI 里常见的节点 | 备注 |
| --- | --- | --- |
| 加载底模及配套文件 | Checkpoint Loader (Simple) | 一个节点同时载入底模与配套的 CLIP、VAE |
| 提示词 → 向量 | CLIP Text Encode | 正向、负向各接一个 |
| 加载 LoRA | LoRA Loader | 接在底模与采样器之间，可串联多个 |
| 去噪全过程 | KSampler | 把采样器、调度器、步数、CFG 打包在一个节点里设置 |
| latent → 图片 | VAE Decode | 缺它不出图 |
| 保存结果 | Save Image | 各版本命名略有差异 |

最容易卡住的一点：概念里的“采样器、调度器、步数、CFG”四个旋钮，在 ComfyUI 里全部收进 KSampler 一个节点的参数——模型卡上写“用 DPM++ 2M Karras、28 步、CFG 7”，填的就是这个节点的那几项。

### 底模、文本编码器、VAE 为什么常被弄混

三个都是“模型文件”，但职责完全不重叠：

- 底模负责“画”：它的权重就是生成能力。
- 文本编码器负责“读”：只处理文字，不碰像素。缺失或不匹配时，模型无法正确理解提示词，工作流可能直接报错。
- VAE 负责“翻译”：只做潜空间与像素图的转换，不参与理解语言，也不能单独生成图。VAE 不合适时，常见症状是颜色发灰、对比度异常、细节不自然。
- 三者的体积量级也不同：底模最大，文本编码器次之，VAE 较小；新一代模型常把三者拆成多个文件发布，缺哪样、症状各不相同——缺文本编码器读不懂提示词，缺 VAE 颜色异常，缺底模则什么都出不来。

分开记的办法：文字进来走编码器，画的过程靠底模和插件，出图时走 VAE。下次遇到“加载失败”或“效果不对”，先问一句：少的是哪个部件？最常见的症状可以先对下表：

| 症状 | 先查什么 | 详见 |
| --- | --- | --- |
| 画面粗糙、细节发糊 | 步数是否太少 | [基本原理](/concepts/how-it-works) |
| 颜色发灰、对比度异常 | VAE 是否缺失或不匹配 | 本页上文 |
| 人物崩坏、肢体错误 | CFG 过高，或 LoRA 权重过高 | [LoRA 入门](/lora/basics) |
| LoRA 完全没反应 | 触发词没写、权重太低、底模不匹配 | [LoRA 入门](/lora/basics) |
| 直接报错、加载失败 | 缺配套文件（文本编码器、VAE）或版本不符 | [常见模型生态](/models/ecosystems) |

### 采样器、步数、CFG、seed 这四个旋钮各拧什么

- **采样器与调度器**：换它们等于换“走法”——同一路程走出来的细节、稳定性和随机感可能不同，没有绝对好坏，常用组合见教程或模型页推荐。模型卡里常见的 Euler、DPM++ 2M Karras 就是采样器（及其调度）的名字。
- **步数（Steps）**：去噪迭代次数。太少画面粗糙、不完整；适中后质量趋于稳定；再多通常只是更慢。不同模型推荐步数不同，Turbo 类模型专门为此减少步数。
- **CFG（Classifier-Free Guidance，无分类器引导）**：模型听提示词的程度。太低自由发挥、容易跑题；太高可能颜色过饱和、结构变差。不同模型敏感度不同，按模型卡推荐值起步。
- **seed（随机种子）**：决定起点的那片初始噪声。固定 seed 才能复现结果、做对照实验。

新手不必逐个穷举组合：先用教程或模型页推荐的一整套参数，再按需要逐个动。

```text
新手起步路径：
教程推荐的模型 + 推荐参数 → 固定 seed 出基准图 → 一次只动一个旋钮 → 记录结论
```

每次只动一格，一格一格建立你对这套工具的手感。

### LoRA 和 ControlNet 插在哪一段

- **LoRA** 挂在底模的权重上，作用于整个去噪过程，改变“画面往哪个方向生成”。它需要匹配底模生态，不是所有模型都能通用。
- **ControlNet** 把一张条件图（线稿、深度图、骨架图等）与提示词一起喂给去噪过程，管“构图和姿势”，不改画风；深入阅读见[ControlNet 入门](/concepts/controlnet)。
- **提示词** 在最上游的文本编码阶段起作用。通常分两部分：**正向提示词**写希望出现的内容，**负向提示词**写希望减少或避免的内容。它更像引导而不是命令，模型不一定完全服从。

三者都在“画”的阶段生效，但对象不同：提示词管语义引导，LoRA 管内容倾向，ControlNet 管空间结构。

### 组件影响谁大谁小：新手先学哪个

部件众多，学习时间有限，排个优先级能省很多弯路。按“改动它对成图的影响”从大到小：

| 层面 | 影响为什么大 | 深入阅读 |
| --- | --- | --- |
| 底模 | 能力的来源，题材与上限由它决定，换它等于换地基 | [底模是什么](/concepts/base-model) |
| 提示词 | 决定能力朝哪个方向发挥，正向负向都要写对 | [Prompt 入门](/prompting/basics) |
| 采样参数 | 影响完成度与质感，但用推荐值就能及格 | 本页上文与[基本原理](/concepts/how-it-works) |
| 放大与后期 | 只补尺寸与细节，改不了内容与结构 | 本页上文放大模型一段 |

这份排序有两层用法：画面不对时，按从上往下的顺序排查；安排学习时，也按从上往下投入时间。常见反例是新手把大量精力花在搜集“神秘参数组合”上，而真正的问题出在底模选错或提示词没写清——参数只能让已有能力发挥得好一点，补不了能力的缺口。

## 最小推演：只改一个变量的对照实验

想验证某个旋钮有没有用，最可靠的办法是：固定其他一切，只改它。假设固定同一句提示词、同一个底模、同一个 seed：

| 对照组 | 固定不变 | 唯一变量 | 观察点 | 预期 |
| --- | --- | --- | --- | --- |
| A1 vs A2 | 提示词、seed、模型、CFG、采样器 | 步数 10 → 30 | 线条与细节完成度 | 30 步明显更完整 |
| B1 vs B2 | 提示词、seed、模型、步数、采样器 | CFG 低 → 高 | 跟词程度与画面自然度 | 更跟词，但过高可能僵硬 |
| C1 vs C2 | 全部参数 | LoRA 开 / 关 | 目标概念是否出现 | 开启后概念出现 |

推演步骤：

1. 第一步永远固定 seed：起点相同，差异才能归因到那个变量。
2. 每轮只改一个旋钮，其余全部保持原样。
3. 记录差异：A 组的差异归给步数，B 组归给 CFG，C 组归给 LoRA。
4. 一次实验只回答一个问题；要比较两个变量，就做两轮实验。
5. 结论只对“这套固定条件”成立：换了模型或分辨率，可能要重新验证。

这个推演只说明“单变量对照”的实验思路，不等于每次改动都有精确可复现的效果——生成过程仍含随机性，模型换代也会让结论过期，但这正是先固定 seed 的原因。

## 对比表

把常见部件当成“旋钮”来理解，下表给出各自的典型影响和新手建议。先对这套“旋钮面板”有整体印象，再逐个上手：

| 部件 / 参数 | 默认作用 | 调大 / 调小的典型效果 | 新手建议 |
| --- | --- | --- | --- |
| 步数 | 去噪迭代次数 | 多：更完整、更慢；少：快但粗糙 | 用模型卡或教程推荐值起步 |
| CFG | 听提示词的强度 | 高：更跟词但易过饱和；低：自由但跑题 | 从推荐值试起，小步调整 |
| 采样器 | 每步去噪怎么走 | 换用改变细节与稳定感，无大小之分 | 用教程推荐的成对组合 |
| 调度器 | 噪声强度随步数衰减 | 与采样器配合影响收敛节奏 | 同上，不单独折腾 |
| LoRA 强度 | 附加概念的影响量 | 高：概念过强画面崩；低：几乎无感 | 按 LoRA 类型参考[LoRA 入门](/lora/basics)的推荐区间，从偏低值试起再上调 |
| seed | 初始噪声来源 | 固定：可复现；随机：探索新图 | 排查问题时固定 |

表里没有底模：它不是旋钮，是地基——只能换，不能调；换底模的影响远大于任何旋钮。调旋钮的前提，永远是底模本身具备对应能力。

## 常见误区

- **“VAE 也能生成图片”**：VAE 只做潜空间与像素图的相互转换，单独一个 VAE 生成不了任何图。
- **“LoRA 可以当底模用”**：LoRA 是对特定底模权重的增量修改，必须挂在匹配的底模上才有效。
- **“参数调好能弥补模型的不足”**：底模没学过的概念，插件和旋钮都补不上（详见[底模是什么](/concepts/base-model)）；旋钮只影响“已有能力怎么发挥”。
- **“文本编码器不用管”**：不同代模型可能用 CLIP、T5 等不同编码器，缺失或不匹配会直接报错或完全不理解提示词。
- **“一次改多个参数更快出结果”**：多变量一起改就无法归因；先用固定 seed 的单变量实验，再逐个叠加结论。
- **“工作流能解决所有问题”**：工作流只是把部件连起来的流程，部件的能力仍是上限。
- **“后期能救一切”**：放大与修图只能补尺寸、细节与噪点，改不了内容画错、结构崩坏这类源头问题；先在前端解决，别把后期当万能补丁。

## 自测问题

### 1. 换采样器和换底模的影响有什么本质区别？

<details>
<summary>查看答案</summary>

底模是能力的来源，决定“会画什么”，换底模等于换地基；采样器只决定去噪过程“怎么走”，是在同一块地基上改变细节与稳定感的表现方式。前者影响内容与上限，后者只影响过程与质感。

</details>

### 2. 出图颜色发灰、对比度异常、细节不自然，应该优先检查哪个部件？

<details>
<summary>查看答案</summary>

优先检查 VAE。VAE 负责潜空间与像素图之间的转换，不匹配或缺失时最典型的症状就是颜色和对比度异常。排除 VAE 之后，再排查模型版本、采样参数等其他因素；如果底模需要搭配独立的 VAE 文件，顺带确认它是否加载正确。

</details>

### 3. 想验证“CFG 是否影响画面”，实验应该怎么设计？

<details>
<summary>查看答案</summary>

固定同一句提示词、同一个底模、同一个 seed 和其他全部参数，只把 CFG 从一个值改到另一个值，比较两张图。起点相同，差异才能归因给 CFG。如果要同时验证步数，应另做一轮实验，不要一次改两个变量。

</details>

### 4. 按“对成图的影响”从大到小排出四个层面，并解释为什么新手不该从搜集参数组合入手。

<details>
<summary>查看答案</summary>

排序：底模 > 提示词 > 采样参数 > 放大与后期。底模决定能力与上限，提示词决定能力朝哪发挥，采样参数在推荐值附近就能及格，后期只补尺寸与细节。参数补不了能力的缺口：底模没学的概念、提示词没写清的要求，换任何参数组合都出不来。所以先把前两层做对，再谈微调参数。

</details>

## 关联阅读

- **前置概念：** [底模是什么](/concepts/base-model)介绍流水线的核心模型。
- **同主题：** [基本原理](/concepts/how-it-works)解释为什么要分步去噪，以及文本条件怎样引导画面。
- **组件深入：** [LoRA 入门](/lora/basics)展开 LoRA 的位置、兼容关系和使用方法。
- **进阶方向：** [ControlNet 入门](/concepts/controlnet)展开条件控制的原理与用法；[精选教程](/resources/tutorials)收录 ComfyUI 官方 ControlNet 教程供上手。
- **提示词写法：** [Prompt 入门](/prompting/basics)包含图像 Prompt 与负向提示词的专门讨论。

## 官方或原始资料来源

- [Stable Diffusion 论文](https://arxiv.org/abs/2112.10752)：潜空间、文本条件与文生图流水线的原始出处。
- [CLIP 论文](https://arxiv.org/abs/2103.00020)：图文对照训练的文本编码器，SD 系列文本编码思路的来源。
- [ControlNet 论文](https://arxiv.org/abs/2302.05543)：给扩散模型加条件控制的原始论文。
- [ControlNet 官方仓库](https://github.com/lllyasviel/ControlNet)：ControlNet 的官方代码仓库。
- [LoRA 论文](https://arxiv.org/abs/2106.09685)：小型附加权重方法的原始出处。
- [Hugging Face Diffusers 文档](https://huggingface.co/docs/diffusers/index)：流水线各部件（编码器、VAE、采样器）的官方实现与说明。
